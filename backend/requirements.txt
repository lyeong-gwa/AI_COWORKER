# FastAPI & Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
alembic==1.13.1
aiosqlite==0.19.0

# Validation & Serialization
pydantic==2.5.3
pydantic-settings==2.1.0

# Code Sandbox (RestrictedPython)
RestrictedPython==7.0

# Vector Database & Embeddings (오프라인 동작)
chromadb==0.4.22
onnxruntime>=1.17.0
tokenizers>=0.15.0
numpy>=1.26.0

# LLM Integration
openai==1.10.0
anthropic==0.18.0

# HTTP Client
httpx==0.26.0

# Utilities
python-dotenv==1.0.0
orjson==3.9.12

# ─────────────────────────────────────────────────────────────────────────────
# [선택] model.onnx 자동 변환용 (pytorch_model.bin만 있는 경우 최초 1회 필요)
# 변환 완료 후에는 아래 패키지 없이도 동작합니다
# pip install optimum[onnxruntime] torch transformers
# ─────────────────────────────────────────────────────────────────────────────
